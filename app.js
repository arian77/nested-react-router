import React from 'react'
import { render } from 'react-dom'

const App = () => {
    return (
        <h2>{'Welcome to React Nested Routing App'}</h2>
    );
};

render((
    <App />
    ),document.getElementById('app'))
